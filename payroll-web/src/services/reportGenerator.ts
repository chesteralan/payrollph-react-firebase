// Stub for reportGenerator — placeholder until the real implementation is created
export const generateReportData = async (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _reportType: string,
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _filters?: Record<string, unknown>,
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _fields?: string[],
): Promise<unknown[]> => {
  return [];
};
