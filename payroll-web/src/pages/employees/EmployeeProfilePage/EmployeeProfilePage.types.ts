import type {
  Employee,
  EmployeeContact,
  EmployeeProfile,
  EmployeeSalary,
  EmployeeDocument,
  DocumentCategory,
} from "../../types";

type ProfileTab = "info" | "contact" | "compensation" | "dtr" | "documents";
